# CGSS Helper

Web frontend for CGSS Helper, built with `React`, `Redux` and `Webpack`.

CGSS, also Cinderella Girl Starlight State, is a idol game developed by Bandai.

## Deployment

Run `npm install && npm run dist` to create dist file. Use nginx or any static server to serve those files.

## Development

Run `npm install` to install all dependencies.
